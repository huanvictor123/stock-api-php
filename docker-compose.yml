version: '3.8'

services:
  php:
    build: .
    container_name: stock_api_php
    ports:
      - "8080:80"
    volumes:
      - ./src:/var/www/html/src
      - ./openapi.yaml:/var/www/html/openapi.yaml
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      - DB_HOST=mysql
      - DB_PORT=3306
      - DB_NAME=${DB_NAME}
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASS}
      - JWT_SECRET=${JWT_SECRET}

  mysql:
    image: mysql:8.0
    container_name: stock_api_mysql
    ports:
      - "3307:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASS}
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASS}
    volumes:
      - mysql_data:/var/lib/mysql
      - ./database/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql
      - ./database/seeds.sql:/docker-entrypoint-initdb.d/02-seeds.sql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 10s
      retries: 5

  swagger:
    image: swaggerapi/swagger-ui
    container_name: stock_api_swagger
    ports:
      - "8081:8080"
    environment:
      URL: http://localhost:8080/openapi.yaml
    depends_on:
      - php

volumes:
  mysql_data:
